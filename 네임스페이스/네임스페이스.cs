using static InTheEndCsharp.Utilities.MathHelper;

namespace InTheEndCsharp.네임스페이스;

public class 네임스페이스
{
    public static void 실행()
    {
        decimal result = Plus(3.3M, 5.5M);
        Console.WriteLine(result);
    }
}